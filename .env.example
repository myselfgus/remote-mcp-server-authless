# Cloudflare Access Configuration
# Application: meta-mcp
# Application ID: a1715203-d8fe-467f-b1b1-054ecb21aa52

# Enable or disable Cloudflare Access authentication
# Set to "false" to disable authentication (for local development)
# Set to "true" to enable authentication (production)
CF_ACCESS_ENABLED=true

# Note: Cloudflare Access validates JWT before reaching the Worker
# We only check if authentication headers are present
# No need to manually validate AUD/team domain anymore
