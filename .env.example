# Cloudflare Access Configuration
# IMPORTANT: Configure Cloudflare Access in the dashboard at:
# https://one.dash.cloudflare.com/ > Access > Applications

# Domain: meta-mcp.voither.workers.dev
# AUD: c3417ca6804a91e05bdd3a054d63d49cdd0e8f2ddef3858589ca2ff0248d3b8c
# JWKs URL: https://voither.cloudflareaccess.com/cdn-cgi/access/certs

# Enable or disable Cloudflare Access
# Set to "false" to disable (for local development)
# Set to "true" to enable (requires dashboard configuration)
CF_ACCESS_ENABLED=true

# NOTE: When CF_ACCESS_ENABLED=true, you MUST have Cloudflare Access
# configured in the dashboard. The dashboard handles ALL authentication.
# The Worker does NOT validate JWT - it trusts Cloudflare Access.
